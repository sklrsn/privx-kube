apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: privx-claim
  namespace: privx
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 3Gi
  storageClassName: manual
